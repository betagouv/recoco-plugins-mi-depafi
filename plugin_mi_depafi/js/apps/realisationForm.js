// TODO(import-map): this plugin now builds independently, so @core/css/*
// can no longer be resolved at build time. select-a11y styles are missing
// until core exposes a shared import map for its stylesheets.
// import "@core/css/select-a11y.scss";

import "../components/RealisationPhotoGallery";
import "../components/RealisationDocumentList";
import "../components/RealisationResourceSelect";
